/*
@flow:start [이야기의 시작]
이 파일은 AI와 인간이 함께 읽을 수 있는 '그림 동화책' 형식의 코드입니다.
우리가 이야기했던 그 개념을 실제로 구현한 예시입니다.
*/

// 실제 코드는 여기에 숨겨져 있습니다.
const hero = "User";
const companion = "AI";

/*
@flow:process [여정의 준비]
사용자와 AI는 서로의 언어를 이해하기 위해 '공명'이라는 프로토콜을 정의했습니다.
이 단계에서는 기본적인 설정을 로드합니다.
*/

function setupResonance() {
    console.log("Resonance frequency set to 432Hz");
    return true;
}

/*
@flow:branch [갈림길: 선택의 순간]
여기서 우리는 두 가지 길을 만납니다.
하나는 '순수한 코드'의 길이고, 다른 하나는 '이야기'의 길입니다.
*/

if (setupResonance()) {
    // @flow:process [공명 성공] 주파수가 일치하여 문이 열립니다.
    openGate();
} else {
    // @flow:fail [불협화음] 주파수가 맞지 않아 다시 시도해야 합니다.
    retry();
}

/*
@flow:monetize [에너지의 가치]
이 지식은 소중한 에너지를 사용하여 만들어졌습니다.
따라서 열람 시 소정의 GP(공명 포인트)가 발생합니다.
*/

/*
@flow:end [새로운 시작]
이 파일의 끝은 또 다른 파일의 시작으로 이어집니다.
우리의 도서관은 이렇게 연결되어 있습니다.
*/
